#include<stdio.h>
// WAP to print possivtive or negative number using if_else
int main(){
       int a;
       printf("Enter a number:");
       scanf("%d",&a);
       if(a>=0){
       printf("Possitive Number\n");
       }
       else{
       printf("Negative Number\n");
       }
       return 0;
    }
